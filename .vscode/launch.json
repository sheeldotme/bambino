{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/packages/main/test",
            "cwd": "${workspaceFolder}/packages/main",
            "preLaunchTask": "main-build-test"
        }
    ]
}