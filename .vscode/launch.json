{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug mandrake-parser tests",
            "program": "${workspaceFolder}/scopes/mandrake/parser/test",
            "cwd": "${workspaceFolder}/scopes/mandrake/parser",
            "preLaunchTask": "mandrake-parser-build-test"
        }
    ]
}